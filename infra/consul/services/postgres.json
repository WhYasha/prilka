{
  "service": {
    "name": "postgres",
    "port": 5432,
    "tags": ["messenger", "database", "postgresql"],
    "check": {
      "tcp": "postgres:5432",
      "interval": "10s",
      "timeout": "5s",
      "deregister_critical_service_after": "30m"
    }
  }
}
